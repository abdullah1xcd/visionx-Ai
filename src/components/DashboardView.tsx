import React,{useEffect,useRef,useState}from'react';
import{DetectionResult}from'../types';import{detectionService}from'../services/detectionService';
export const DashboardView=()=>{const canvas=useRef<HTMLCanvasElement>(null);const[results,setResults]=useState<DetectionResult[]>([]);const[status,setStatus]=useState('READY');useEffect(()=>{void detectionService.initModel().then(()=>setStatus(detectionService.getStatus().status))},[]);return <div className="p-6 text-white bg-[#080A0D] h-full"><h2 className="text-xl font-black">LEGACY DASHBOARD</h2><p className="text-xs text-[#9AA4AF]">AI engine: {status}</p><canvas ref={canvas} className="hidden"/><div className="mt-4 text-xs">{results.length} real detections in the current frame.</div></div>};
